services:
  apt-cache-proxy:
    build: .
    container_name: apt-cache-proxy
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      # Persist the downloaded cache packages
      - ./storage:/app/storage
      # Persist the SQLite statistics database and config
      - ./data:/app/data
